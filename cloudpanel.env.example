# CloudPanel Production Environment Variables
# Store this file securely at: /etc/wick-wax-relax/cloudpanel.env
# Generate strong secrets using: openssl rand -base64 32

# Required Database Configuration
DOMAIN="your-production-domain.com"
DB_NAME="wick_wax_relax"
DB_USER="wick_wax_prod_user"
DB_PASSWORD="$(openssl rand -base64 24)"  # Generate random password
CLOUDPANEL_PUBLIC_PATH="/srv/cloudpanel/htdocs/${DOMAIN}/public"  # Update to the public dir CloudPanel created for this site

# Security Secrets (Rotate these periodically)
JWT_SECRET="$(openssl rand -base64 48)"
JWT_REFRESH_SECRET="$(openssl rand -base64 48)"

# Optional Advanced Settings
# NODE_ENV="production"
# REDIS_URL="redis://redis:6379"
# SSL_CERT_PATH="/etc/letsencrypt/live/${DOMAIN}"